import { ArraySortPipe } from './array-sort.pipe';

describe('ArraySortPipe', () => {
  it('create an instance', () => {
    const pipe: any = new ArraySortPipe();
    expect(pipe).toBeTruthy();
  });
});
